/**
 * Konfiguracja środowiska dla aplikacji.
 *
 * @property {boolean} production - Flaga wskazująca, czy aplikacja działa w trybie produkcyjnym.
 * @property {string} apiUrl - Adres URL API backendu.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
